repositories:

releases:
  - name: go-app
    namespace: go-app
    labels:
      component: app
    chart: './chart'
    version: 0.1.0
    secrets:
      - values/development/encrypted_secrets.yaml

